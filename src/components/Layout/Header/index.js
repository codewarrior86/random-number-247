import styles from "./Header.module.css";

const Header = () => {
    return (
        <nav className={styles.header}>
            <div className={styles.name}>Muhammad Arjun</div>
        </nav>
    )
}

export default Header;